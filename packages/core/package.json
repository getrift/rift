{
  "name": "@rift/core",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config ./vitest.config.mts",
    "lint": "echo '@rift/core lint not configured'"
  },
  "dependencies": {
    "tslib": "^2.6.2",
    "yaml": "^2.4.2"
  }
}
